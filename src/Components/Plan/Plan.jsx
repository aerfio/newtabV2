import React from 'react';
import plan from './planik.PNG';
import './Plan.css';
const Plan = () => {
	return (
		<div className={'plan-container'}>
			<img src={plan} />;
		</div>
	);
};

export default Plan;
